{
  "name": "ahc-firestore-rules-tests",
  "version": "1.0.0",
  "description": "Tests de Firestore Security Rules contra el Firebase Emulator",
  "scripts": {
    "test": "jest --testEnvironment node --forceExit",
    "test:watch": "jest --testEnvironment node --watch"
  },
  "jest": {
    "testTimeout": 30000
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-admin": "^12.0.0",
    "jest": "^29.0.0"
  }
}
